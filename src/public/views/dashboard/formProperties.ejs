<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title>
        <%= isEdit ? 'Edit Property' : 'Create Property' %> - <%= config.Business.Name %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="font-sans bg-gray-100">
    <div class="flex min-h-screen relative">
        <%- include('../partials/aside') %>
            <div class="flex-1 flex flex-col transition-all duration-300 ease-in-out md:ml-64 w-full">
                <%- include('../partials/header') %>
                    <main class="flex-1 p-4 sm:p-6 lg:p-8 overflow-y-auto">
                        <h1 class="text-xl sm:text-2xl md:text-3xl font-bold text-center mb-6 sm:mb-8">
                            <%= isEdit ? 'Edit Property' : 'Create Property' %>
                        </h1>
                        <form id="<%= isEdit ? 'editPropertyForm' : 'addPropertyForm' %>"
                            action="<%= isEdit ? `/api/property/update/${property?.id}` : '/api/property/create' %>"
                            method="POST" enctype="multipart/form-data" class="space-y-4">
                            <input type="hidden" name="id" value="<%= property?.id || '' %>" />
                            <div>
                                <label for="name" class="block text-sm font-medium">Name</label>
                                <input type="text" id="name" name="name" value="<%= property?.name || '' %>"
                                    class="w-full border rounded-lg p-2" required />
                            </div>
                            <div>
                                <label for="description" class="block text-sm font-medium">Description</label>
                                <textarea id="description" name="description" rows="4"
                                    class="w-full border rounded-lg p-2"
                                    required><%= property?.description || '' %></textarea>
                            </div>
                            <div>
                                <label for="price" class="block text-sm font-medium">Price</label>
                                <input type="number" id="price" name="price" value="<%= property?.price || '' %>"
                                    class="w-full border rounded-lg p-2" required />
                            </div>
                            <div>
                                <label for="currencySymbol" class="block text-sm font-medium">Currency Symbol</label>
                                <input type="text" id="currencySymbol" name="currencySymbol"
                                    value="<%= property?.currencySymbol || '' %>" class="w-full border rounded-lg p-2"
                                    minlength="1" maxlength="3" required />
                            </div>
                            <div>
                                <label for="hours" class="block text-sm font-medium">Hour(s)</label>
                                <input type="number" id="hours" name="hours" value="<%= property?.hours || '' %>"
                                    class="w-full border rounded-lg p-2" required />
                            </div>
                            <div>
                                <label for="address" class="block text-sm font-medium">Address</label>
                                <input type="text" id="address" name="address" value="<%= property?.address || '' %>"
                                    class="w-full border rounded-lg p-2" required />
                            </div>
                            <div>
                                <label for="featuredImage" class="block text-sm font-medium">Featured Image</label>
                                <input type="file" id="featuredImage" name="featuredImage"
                                    class="w-full border rounded-lg p-2" />
                                <% if (property?.featuredImage) { %>
                                    <img src="data:image/jpeg;base64,<%= property.featuredImage %>" alt="Featured Image"
                                        class="mt-2 w-32 h-32 object-cover rounded-lg" />
                                    <% } %>
                            </div>
                            <div>
                                <label for="images" class="block text-sm font-medium">Additional Images</label>
                                <input type="file" id="images" name="images" class="w-full border rounded-lg p-2"
                                    multiple />
                                <% if (property?.images && property.images.length> 0) { %>
                                    <div class="mt-2 flex space-x-2">
                                        <% property.images.forEach((image)=> { %>
                                            <img src="data:image/jpeg;base64,<%= image %>" alt="Additional Image"
                                                class="w-16 h-16 object-cover rounded-lg" />
                                            <% }); %>
                                    </div>
                                    <% } %>
                            </div>
                            <div>
                                <label for="showcase" class="block text-sm font-medium">Showcase</label>
                                <input type="checkbox" id="showcase" name="showcase" class="w-6 h-6"
                                    <%=property?.showcase ? 'checked' : '' %> />
                            </div>
                            <button type="submit"
                                class="<%= isEdit ? 'bg-blue-500 hover:bg-blue-600' : 'bg-green-500 hover:bg-green-600' %> 
                                   text-white px-4 py-2 rounded-lg w-full transition flex items-center justify-center space-x-2">
                                <i class="<%= isEdit ? 'fas fa-save' : 'fas fa-plus' %>"></i>
                                <span>
                                    <%= isEdit ? 'Save Changes' : 'Create Property' %>
                                </span>
                            </button>
                        </form>
                    </main>
            </div>
    </div>
</body>

</html>