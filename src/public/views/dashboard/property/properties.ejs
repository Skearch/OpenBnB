<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no" />
  <title>Properties - <%= config.Business.Name %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet" />
</head>

<body class="font-sans bg-gray-100">
  <div class="flex min-h-screen relative">
    <div id="sidebarOverlay" class="fixed inset-0 bg-black bg-opacity-50 z-30 hidden md:hidden" aria-hidden="true">
    </div>
    <%- include('../../partials/aside') %>
      <div class="flex-1 flex flex-col transition-all duration-300 ease-in-out md:ml-64 w-full">
        <%- include('../../partials/header') %>
          <main class="flex-1 p-4 sm:p-6 lg:p-8 overflow-y-auto">
            <h1 class="text-xl sm:text-2xl md:text-3xl font-bold text-center mb-6 sm:mb-8">
              Manage Properties
            </h1>
            <div class="bg-white shadow rounded-lg p-4 sm:p-6 hover:shadow-xl transition-shadow">
              <table class="w-full border-collapse border border-gray-300 text-sm sm:text-base">
                <thead class="bg-gray-100">
                  <tr>
                    <th class="border border-gray-300 px-2 py-1 text-left">
                      Name
                    </th>
                    <th class="border border-gray-300 px-2 py-1 text-left">
                      Price
                    </th>
                    <th class="border border-gray-300 px-2 py-1 text-left">
                      Showcase
                    </th>
                  </tr>
                </thead>
                <tbody id="property-list">
                  <tr id="no-properties-row" class="hidden">
                    <td colspan="3" class="text-center text-gray-500 py-6">No properties found.</td>
                  </tr>
                </tbody>
              </table>

              <div class="mt-6 flex flex-col sm:flex-row justify-center space-y-3 sm:space-y-0 sm:space-x-6">
                <a href="/dashboard/properties/create"
                  class="bg-green-500 w-full text-white text-center px-4 py-2 rounded-lg hover:bg-green-600 transition flex items-center justify-center space-x-2">
                  <i class="fas fa-plus"></i>
                  <span>Create</span>
                </a>
                <button id="edit-button"
                  class="bg-blue-500 w-full text-white px-4 py-2 rounded-lg hover:bg-blue-600 transition disabled:bg-gray-400 disabled:cursor-not-allowed flex items-center justify-center space-x-2"
                  disabled>
                  <i class="fas fa-edit"></i>
                  <span>Edit</span>
                </button>
                <button id="clone-button"
                  class="bg-yellow-500 w-full text-white px-4 py-2 rounded-lg hover:bg-yellow-600 transition disabled:bg-gray-400 disabled:cursor-not-allowed flex items-center justify-center space-x-2"
                  disabled>
                  <i class="fas fa-copy"></i>
                  <span>Clone</span>
                </button>
                <button id="delete-button"
                  class="bg-red-500 w-full text-white px-4 py-2 rounded-lg hover:bg-red-600 transition disabled:bg-gray-400 disabled:cursor-not-allowed flex items-center justify-center space-x-2"
                  disabled>
                  <i class="fas fa-trash-alt"></i>
                  <span>Delete</span>
                </button>
              </div>
            </div>
          </main>
      </div>
  </div>

  <script src="/js/dashboard/property/properties.js"></script>
  <script src="/js/script.js"></script>
</body>

</html>