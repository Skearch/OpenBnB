<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Email Verification - <%= config.Business.Name %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
    <link href="/css/style.css" rel="stylesheet" />
</head>

<body class="font-sans bg-gray-50 min-h-screen flex flex-col">
    <%- include('../partials/navbar') %>
        <main class="flex-1 flex items-center justify-center py-16 px-4">
            <section class="w-full max-w-md bg-white shadow-lg rounded-lg p-8">
                <h1 class="text-2xl font-bold text-center mb-6">Verify Your Email</h1>
                <form id="verificationForm" class="space-y-4">
                    <div>
                        <label for="code" class="block text-sm font-medium mb-1">Verification Code</label>
                        <input type="text" id="code" name="code" maxlength="6" pattern="[0-9]{6}"
                            class="w-full border rounded-lg p-2 text-center tracking-widest text-lg" required />
                    </div>
                    <input type="hidden" id="email" name="email" value="<%= email %>" />
                    <div id="verification-error" class="text-red-500 text-sm hidden"></div>
                    <button type="submit" class="custom-call-to-action w-full py-2 rounded-lg font-semibold">
                        Verify
                    </button>
                </form>
                <p class="text-center mt-4 text-sm">
                    Didn't get a code? <a href="#" class="text-blue-500 hover:underline" id="resend-code">Resend</a>
                </p>
            </section>
        </main>
        <%- include('../partials/footer') %>
            <script src="/js/verification.js"></script>
</body>

</html>