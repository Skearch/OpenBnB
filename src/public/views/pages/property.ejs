<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Property Details - <%= config.Business.Name %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
  <link href="../css/style.css" rel="stylesheet" />
</head>

<body class="font-sans bg-gray-50 min-h-screen flex flex-col">
  <%- include('../partials/navbar') %>
    <main class="flex-1 py-16 px-4">
      <section class="max-w-4xl mx-auto">
        <div id="property-details" data-property-id="<%= propertyId %>" data-check-in-time="<%= property.checkInTime %>"
          data-check-out-time="<%= property.checkOutTime %>" data-price="<%= property.price %>"
          class="bg-white shadow rounded-lg p-8 flex flex-col md:flex-row gap-8 mb-10 min-h-[400px] items-stretch">
          <div class="flex-1 flex flex-col h-full justify-start"></div>
          <div class="flex-1 flex flex-col h-full"></div>
        </div>
        <div class="max-w-4xl mx-auto bg-white shadow rounded-lg p-8 flex flex-col md:flex-row gap-8">
          <% if (user) { %>
            <div class="w-full md:w-1/2 flex justify-center">
              <div id="booking-calendar-container" class="w-full max-w-xs"></div>
            </div>
            <div class="w-full md:w-1/2 flex justify-center">
              <div id="booking-summary" class="w-full max-w-xs">
                <div id="booking-summary-static" class="space-y-4 bg-gray-50 rounded-lg p-4 shadow-inner">
                  <div class="text-gray-900 text-base md:text-lg">
                    <span class="font-semibold">Check-in:</span>
                    <b id="summary-checkin" class="font-normal text-gray-700">-</b>
                  </div>
                  <div class="text-gray-900 text-base md:text-lg">
                    <span class="font-semibold">Check-out:</span>
                    <b id="summary-checkout" class="font-normal text-gray-700">-</b>
                  </div>
                  <div class="text-gray-900 text-base md:text-lg">
                    <span class="font-semibold">Total night(s):</span>
                    <b id="summary-nights" class="font-normal text-gray-700">-</b>
                  </div>
                  <div class="text-gray-900 text-base md:text-lg">
                    <span class="font-semibold">Total amount:</span>
                    <b id="summary-amount" class="font-normal text-gray-700">-</b>
                  </div>
                  <button id="book-now"
                    class="custom-call-to-action bg-green-600 text-black px-6 py-3 w-full rounded-lg font-semibold text-lg transition disabled:opacity-50 disabled:cursor-not-allowed hover:bg-green-700"
                    disabled>
                    Book Now
                  </button>
                </div>
              </div>
            </div>
            <% } else { %>
              <div class="w-full flex flex-col items-center justify-center min-h-[200px]">
                <div class="text-gray-900 text-xl font-semibold mb-4 text-center">
                  You must be logged in to book
                </div>
                <a href="/account/login?<%= 'rdirect=' + encodeURIComponent(request.originalUrl || '/property/' + propertyId) %>"
                  class="block w-full max-w-xs">
                  <button
                    class="bg-blue-600 text-white px-6 py-3 rounded-lg w-full text-lg font-semibold hover:bg-blue-700 transition">
                    Sign in
                  </button>
                </a>
              </div>
              <% } %>
        </div>
      </section>
    </main>
    <%- include('../partials/footer') %>
      <script src="../js/pages/property.js"></script>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>